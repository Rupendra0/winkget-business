"use client";

import SectionPlaceholder from "@/components/admin/SectionPlaceholder";

export default function EmployeesPage() {
  return <SectionPlaceholder title="Employees" subtitle="Manage employee records, attendance, and reports." />;
}
