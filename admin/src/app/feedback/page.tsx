"use client";

import SectionPlaceholder from "@/components/admin/SectionPlaceholder";

export default function FeedbackPage() {
  return <SectionPlaceholder title="Feedback & Disputes" subtitle="Support requests, disputes, and feedback actions." />;
}
