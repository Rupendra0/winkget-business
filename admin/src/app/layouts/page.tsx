"use client";

import SectionPlaceholder from "@/components/admin/SectionPlaceholder";

export default function LayoutsPage() {
  return <SectionPlaceholder title="Layouts & Templates" subtitle="Manage templates and layout selections." />;
}
