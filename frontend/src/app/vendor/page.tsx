export default function VendorPage() {
  return (
    <main className="min-h-[calc(100vh-80px)] px-4 sm:px-6 lg:px-8 py-10">
      <section className="mx-auto max-w-5xl rounded-3xl bg-white/85 border border-white/80 shadow-2xl p-8 sm:p-10">
        <h1 className="text-3xl sm:text-4xl font-bold text-slate-900">Vendor Dashboard</h1>
        <p className="mt-3 text-slate-600">
          Vendor login successful. This is the /vendor page where we will build your vendor panel next.
        </p>
      </section>
    </main>
  );
}
